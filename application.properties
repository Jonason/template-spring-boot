# ----------------------------------------
# PROPERTIES
# ----------------------------------------
# management.security.enabled is set to false for testing purpose only
# This should be reverted in any actual application
management.security.enabled=false
#
# Configure info endpoint
info.name=Template Spring Boot 
info.description=Template to demonstrate Spring Boot
info.version=1.0.0
info.java-runtime = ${java.runtime.name}
#
# enable/disable endpoints
#
# endpoints.health.enabled=true
# endpoints.health.sensitive=false
# enable the metrics endpoint
# endpoints.metrics.enabled=true
# endpoints.metrics.sensitive=false
# endpoints.env.enabled=true
# endpoints.env.sensitive=false
# endpoints.trace.enabled=true
# endpoints.trace.sensitive=false
# endpoints.beans.enabled=true
# endpoints.beans.sensitive=false
# endpoints.configprops.enabled=true
# endpoints.configprops.sensitive=false
# endpoints.dump.enabled=true
# endpoints.dump.sensitive=false
# endpoints.autoconfig.enabled=true
# endpoints.autoconfig.sensitive=false
# endpoints.mappings.enabled=true
# endpoints.mappings.sensitive=false